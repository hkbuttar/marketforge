# MarketForge runtime
duckdb>=1.0,<2
pytz>=2024
PyYAML>=6,<7
hypothesis>=6,<7

# Query serving
fastapi>=0.115,<1
uvicorn>=0.30,<1
httpx2>=2,<3

# Transformation
dbt-core>=1.10,<1.11
dbt-duckdb>=1.10,<1.11

# Orchestration and local UI
dagster>=1.10,<2
dagster-webserver>=1.10,<2

# Optional StreamAlpha bridge (the core platform does not require Kafka)
# confluent-kafka>=2.5,<3
